# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'Moviezella' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Moviezella

    pod 'Alamofire', '~> 5.0'
    pod 'Kingfisher', '~> 5.0'
    pod "SkeletonView", '~> 1.8.7'
end

plugin 'cocoapods-keys', {
:project => "Moviezella",
:keys => [
  "MoviesApiKeys"
]}
